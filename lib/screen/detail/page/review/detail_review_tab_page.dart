import 'package:flutter/material.dart';

class DetailReviewTabPage extends StatelessWidget {
  const DetailReviewTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Review Tab View"),
    );
  }
}